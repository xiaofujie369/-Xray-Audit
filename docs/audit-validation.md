# 部署验证状态

当前版本为 2.1.0-dev。本次第二版按维护者指示不执行本地测试、构建、Compose/Xray 配置验证或真实 AI 请求。新增 V2 回归用例和迁移随代码交付，但没有本轮运行结果；不声明通过生产验收。

以下仅为 V1 先前开发阶段的记录，不能证明 V2 通过验证：原有同步/上报测试、Agent/后台/分析测试、官方 Xray 26.5.9 配置及真实 VLESS 转发测试；真实 Compose + PostgreSQL + Redis + Celery 的并发去重、多 VPS 搜索、探测共识、关联计算、恢复、数据库停机补传和 Redis 停机入库测试；独立 Ubuntu 24.04 下全新节点安装及从原版 5b82623 同步/上报代码原地升级，升级前后配置摘要一致。

最后一批变更未完成回归。仍需在部署环境确认 Agent/Probe 注册与 systemd 长期运行、完整备份恢复、Caddy 公网证书、规模压力与长时间稳定性。GitHub Actions 工作流已随代码提供，本次交付不以 CI 通过作为完成声明。

V2 部署需要实际确认：从 0002 升级到 0003、历史基线回填、旧/新 Probe 混合期间保持疑似、境外和健康心跳对照、晚到数据重算、明细到期后快照保留、AI 脱敏请求与额度/失败恢复、备份恢复及前端报告引用。参见 [V2 指南](audit-v2.md)。
